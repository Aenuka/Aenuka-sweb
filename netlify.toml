[build]
  publish = "dist"
  functions = "src/functions"
  command = "npm run build"

[dev]
  publish = "dist"
  functions = "src/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
